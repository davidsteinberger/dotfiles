#!/bin/bash


echo
echo
echo "Start Vim with 'vim', ignore errors and run the command 'NeoBundleUpdate'"
echo "Quit Vim afterwards and run 'cd ~/.vim/bundle/YouCompleteMe; git submodule update --init --recursive; ./install.sh --clang-completer;'"
echo "Start tmux with 'tmux', then run the command 'source-file ~/.tmux.conf'"
