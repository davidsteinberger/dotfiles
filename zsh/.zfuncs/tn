local session
session=$(tmux list-sessions -F "#{session_name}" 2>/dev/null | fzf --prompt "Session: " --height=~50% --no-multi --exit-0) || read "session?New session name: "
[[ -n "$session" ]] && tmux new-session -A -s "$session"
