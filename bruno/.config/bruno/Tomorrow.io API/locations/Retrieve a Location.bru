meta {
  name: Retrieve a Location
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/locations/:locationId
  body: none
  auth: inherit
}

params:path {
  locationId: {{locationId}}
}

script:pre-request {
  var locationId = "5fb7dc8fd4f7cd0008a64456";
  
  bru.setEnvVar('locationId', locationId);
}

settings {
  encodeUrl: true
}
